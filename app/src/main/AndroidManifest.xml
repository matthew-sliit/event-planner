<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.testapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Budget"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:node="replace">

        <activity android:name=".EditVendor"/>
        <activity android:name=".VendorView" />
        <activity android:name=".NumberStandardActivity"/>
        <activity android:name=".MainActivityEmu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddVendor" />
        <activity android:name=".EditPayment"/>
        <activity android:name=".AddPayment" />
        <activity android:name=".EditCategoryActivity" />
        <activity android:name=".AppSettingsActivity" />
        <activity android:name=".ListBudgetsActivity" />
        <activity android:name=".GraphActivity" /> <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".AddBudgetActivity" />
    </application>

</manifest>
